//WAP to check if number is positive, negative or neutral (0).
#include<stdio.h>
void main(){
	int num=0;
	
	if(num==0)
		printf("Neutral (0)");
	else if(num>0)
		printf("Positive");
	else
		printf("Negative");
}